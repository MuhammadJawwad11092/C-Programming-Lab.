#include <stdio.h>

float average(int x, int y, int z) {
    return (x + y + z) / 3.0;
}

int main() {
    int num1 = 10;
    int num2 = 20;
    int num3 = 30;

    float avg = average(num1, num2, num3);
    printf("Average = %.2f\n", avg);

    return 0;
}
